/tmp/phprd$ sudo make install
Installing shared extensions:     /usr/lib/php/20190902

Create an .ini as such (You have to use an editor.  This should be the result below.)
/etc/php/7.4/mods-available$ cat rdtscp.ini
extension=rdtscp.so

/etc/php/7.4/mods-available$ 

Run:
sudo phpenmod rdtscp
sudo service apache2 restart

I think those commands / files cover it.
